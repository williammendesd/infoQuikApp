import 'package:flutter/material.dart';

ThemeData myTheme = ThemeData(
  // primaryColor: Color.fromRGBO(0, 72, 142, 1),
  brightness: Brightness.light,
  useMaterial3: true,
  // colorScheme: ColorScheme.fromSwatch(primarySwatch: Colors.indigo,),
);